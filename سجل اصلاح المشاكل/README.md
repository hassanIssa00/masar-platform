# سجل اصلاح المشاكل

آخر تحديث: 2026-09-02

هذا السجل يوثق إصلاحات مشاكل الربط والمزامنة والتسجيل والواجهات التي ظهرت في منصة مسار وفصل د. إسماعيل.

## 1. مشكلة الطالب لا يظهر في إدارة الطلاب

المشكلة:
كان إنشاء حساب طالب أو توليد حسابات أحياناً يحفظ الحساب فقط، أو يحفظ ملف طالب غير مكتمل، ثم لا يظهر بشكل ثابت في صفحة إدارة الطلاب.

الإصلاح:
- تم نقل التخزين العملي إلى `cloudStore` وربط الحفظ بمجموعات Firestore.
- تم التأكد أن توليد الحسابات يكتب ملف الطالب في مجموعة `students`.
- تم توسيع مفاتيح المزامنة المستخدمة في صفحات الطلاب والتقارير.
- تم إبطال كاش snapshot بعد عمليات الحفظ والحذف.

الملفات الأساسية:
- `src/lib/cloudStore.ts`
- `src/lib/firestoreSync.ts`
- `src/app/api/accounts/generate/route.ts`
- `src/app/api/data/snapshot/route.ts`
- `src/app/students/page.tsx`

## 2. مشكلة ولي الأمر لا يتعرف على الابن

المشكلة:
حساب ولي الأمر كان أحياناً لا يلتقط الطالب الموجود، أو ينشئ طالباً مكرراً ويربط عليه، خصوصاً مع اختلاف كتابة الأسماء العربية أو أرقام الهاتف.

الإصلاح:
- تم تحسين مطابقة الاسم العربي بتطبيع الهمزات والتاء المربوطة والألف المقصورة.
- تم اعتماد `linkedStudentId` و`linkedStudentEmail` كأقوى إشارات الربط.
- تم دعم ربط الأب بالطالب عن طريق الهاتف، البريد، اسم الأب، واسم الطالب.
- تم منع fallback خطر كان يختار أول طالب إذا لم يجد مطابقة.

الملفات الأساسية:
- `src/lib/nameMatching.ts`
- `src/lib/auth/session.server.ts`
- `src/app/api/auth/session/route.ts`
- `src/app/school-parent/page.tsx`
- `src/app/parent/page.tsx`

## 3. مشكلة التوليد يدخل البوابة بدون بيانات أو استبيان

المشكلة:
مجرد وجود رابط بين حساب الطالب وحساب ولي الأمر كان يجعل `onboardingRequired` يساوي false، فيدخل الحساب إلى البوابة قبل استكمال بيانات الطالب أو الاستبيان أو اختبار الطالب.

الإصلاح:
- تم فصل معنى "الحساب مربوط" عن معنى "الحساب مكتمل".
- حساب ولي الأمر يظل محتاج استكمال حتى تكون بيانات الطالب مكتملة ويوجد استبيان محفوظ.
- حساب الطالب يظل محتاج استكمال حتى تكون بيانات الطالب مكتملة ويوجد اختبار طالب محفوظ.
- التسجيل العادي وجوجل وآبل وحسابات التوليد تدخل أولاً إلى `/student/new` ثم `/survey` أو `/assessment` حسب الدور.
- لو الحساب مرتبط لكن لا يوجد ملف طالب مكتمل أو لا توجد إجابات مطلوبة، يعاد توجيهه لمسار الإكمال بدلاً من دخول البوابة.

الملفات الأساسية:
- `src/app/auth/login/page.tsx`
- `src/app/auth/register/page.tsx`
- `src/app/api/auth/register/route.ts`
- `src/lib/auth/session.server.ts`
- `src/app/api/auth/session/route.ts`
- `src/app/student/new/page.tsx`
- `src/app/survey/page.tsx`
- `src/app/assessment/page.tsx`

## 4. مشكلة النص في تسجيل الدخول يفترض طفلاً واحداً

المشكلة:
الواجهة كانت تكتب "طفلك" و"تم التعرف على طفلك"، وهذا غير مناسب لحساب ولي أمر له أكثر من طالب.

الإصلاح:
- تم تعديل النصوص إلى صياغة عامة: "ملف طالب مرتبط" و"الطلاب".
- الربط الداخلي ما زال يعمل تلقائياً، لكن الواجهة لا تعرض افتراضاً بوجود طفل واحد.

الملفات الأساسية:
- `src/app/auth/register/page.tsx`
- `src/app/student/new/page.tsx`
- `src/components/Navbar.tsx`

## 5. مشكلة الاعتماد على التخزين المحلي

المشكلة:
كانت بيانات كثيرة تقرأ من طبقة محلية قديمة، مما يجعل كل جهاز يرى بيانات مختلفة أو يعيد بيانات محذوفة من الكاش.

الإصلاح:
- تم استبدال الاعتماد على `localDb` بطبقة `cloudStore`.
- تم حذف `src/lib/localDb.ts`.
- تم ربط القراءة والكتابة بواجهات API وFirestore snapshot.
- تم جعل حذف الطلاب يحذف السجلات المرتبطة من السحابة أيضاً.

الملفات الأساسية:
- `src/lib/cloudStore.ts`
- `src/lib/firestoreSync.ts`
- `src/app/api/data/doc/route.ts`
- `src/app/api/data/purge/route.ts`
- `src/app/api/students/purge/route.ts`

## 6. مشكلة تقارير PDF والعرض والطباعة

المشكلة:
التقرير كان يفتح من منتصف الصفحة، ونسخة العرض ليست نفس نسخة الطباعة، والتوقيع والختم أحياناً يظهران في صفحة إضافية فارغة، وصورة الطالب لا تظهر دائماً.

الإصلاح:
- تم جعل عرض التقرير يعتمد على مكون الطباعة نفسه.
- تم إجبار صفحة التقرير على الفتح من الأعلى.
- تم تحسين إدراج صورة الطالب من ملف الطالب أو التقرير.
- تم تقليل الصفحات الزائدة ووضع التوقيع والختم في آخر صفحة محتوى قدر الإمكان.

الملفات الأساسية:
- `src/app/reports/page.tsx`
- `src/app/reports/[id]/page.tsx`
- `src/components/ReportPrintDocument.tsx`
- `src/components/PrintableReportModal.tsx`

## 7. مشكلة السايدبار في الموبايل

المشكلة:
السايدبار في عرض الموبايل لم يكن ثابتاً بشكل مريح، وكان ممكن يتداخل مع الصفحة أو تتحرك الخلفية أثناء فتحه.

الإصلاح:
- تم ضبط عرض السايدبار على حجم شاشة الموبايل.
- تم قفل scroll الخلفية أثناء فتحه.
- تم إغلاقه تلقائياً بعد الانتقال لأي رابط.
- تم رفعه فوق محتوى الصفحة بطبقة ثابتة.
- تم ضبط سايدبار فصل د. إسماعيل المستقل بنفس منطق الموبايل حتى لا تختفي القائمة أو تغطي الصفحة بشكل خاطئ.

الملفات الأساسية:
- `src/components/Sidebar.tsx`
- `src/components/Navbar.tsx`
- `src/app/branches/ikhlas-jeddah/page.tsx`

## 8. مشكلة معرفة تقدم الطالب في المسار

المشكلة:
بعد اعتماد مسار للطالب لم تكن هناك شاشة واضحة تعرض ما فتحه الطالب، أين وصل، وما الواجبات أو الصفحات التي أنجزها.

الإصلاح:
- تم إضافة تتبع فتح صفحات المنهج وحفظ الرسومات بالقلم.
- تم إنشاء مجموعات سحابية للتكليفات ورسومات الطالب وحركة التعلم.
- تم إضافة لوحة تقدم داخل ملف الطالب في `/students`.
- تم منع فتح الدكتور للمنهج من الاحتساب كتقدم للطالب.

الملفات الأساسية:
- `src/lib/learningProgress.ts`
- `src/components/CurriculumInteractiveWorkbook.tsx`
- `src/app/students/page.tsx`
- `src/lib/firestoreSync.ts`
- `src/app/api/data/doc/route.ts`
- `src/app/api/data/snapshot/route.ts`

## 9. مشكلة الشات والرسائل لا تحتفظ بالصوت أو النص

المشكلة:
المحادثات كانت محتاجة أن تحتفظ بالرسائل ولا تضيع، وأن تدعم إرسال تسجيل صوتي.

الإصلاح:
- تم إضافة مكون تسجيل صوتي موحد.
- تم دعم حفظ `audioDataUrl` داخل الرسائل.
- تم تطبيقه في رسائل المنصة، صفحة أولياء الأمور، الشات المجتمعي، الاجتماعات، والبث المباشر.

الملفات الأساسية:
- `src/components/VoiceRecorderButton.tsx`
- `src/lib/cloudStore.ts`
- `src/app/messages/page.tsx`
- `src/app/parents/page.tsx`
- `src/app/parent/page.tsx`
- `src/components/ParentsCommunityChatTab.tsx`
- `src/app/meetings/page.tsx`
- `src/components/LiveStreamTab.tsx`

## 10. مشكلة حسابات التوليد بعد تسجيل الخروج والربط

المشكلة:
بعض حسابات الطالب وولي الأمر المولدة كانت تسجل الدخول أول مرة ثم بعد الخروج تعود صفحة الدخول أو تدخل بحساب ناقص الربط، كما أن حساب ولي الأمر كان يفتح شاشة إدخال اسم/صورة الطالب رغم أن الحساب مولد ومفترض أنه مرتبط بالطالب.

الإصلاح:
- تم إضافة مسار خروج سيرفر يمسح Cookie جلسة `masar_session` فعلياً.
- تم جعل صفحة الدخول تمرر `student=id` عند وجود رابط طالب، بدلاً من الاعتماد على التخمين بعد الدخول.
- تم إخفاء خانة اسم الطالب ورفع الصورة من شاشة ولي الأمر عندما يكون الطالب معروفاً من `linkedStudentId`.
- تم حفظ الاستبيان نفسه على السحابة، وليس التقرير فقط، حتى لا يرجع ولي الأمر لشاشة البيانات بعد إكمال الاستبيان.
- تم ترميم الحسابات المولدة القديمة: إذا كان الحساب محفوظاً في Firebase Auth لكن وثيقة الحساب/الطالب ناقصة روابط، يقوم السيرفر بإعادة بناء `linkedStudentId` و`linkedParentId` من وثائق الحسابات والطلاب ويحفظها.
- تم إصلاح Firebase fallback ليبحث عن وثيقة الحساب بالإيميل وليس بالـ UID فقط.

الملفات الأساسية:
- `src/app/api/auth/logout/route.ts`
- `src/lib/cloudStore.ts`
- `src/lib/auth/session.server.ts`
- `src/app/auth/login/page.tsx`
- `src/app/student/new/page.tsx`
- `src/app/survey/page.tsx`
- `src/app/school-parent/page.tsx`

التحقق:
- تم تشغيل `npm run build` بنجاح.
- تم النشر على `https://masarplatform.org`.
- تم اختبار حساب طالب مولد قديم، وحساب ولي أمر مولد قديم، وحساب طالب مولد جديد، وحساب ولي أمر مولد جديد؛ الأربعة رجعوا من API تسجيل الدخول بروابط `linkedStudentId / linkedParentId` كاملة.
## 11. إصلاح شامل لمسار تسجيل الدخول والاستبيان وصفحة البيانات

المشكلة:
- صفحة ملء البيانات كانت تظهر كل مرة يسجل فيها ولي الأمر دخوله لأن `onboardingRequired` كان يُحفظ بقيمة `true` حتى بعد إكمال البيانات.
- الاستبيان كان لا يظهر لأن صفحتي ولي الأمر (parent وschool-parent) كانتا تعرضان لوحة التحكم مباشرة بدون التحقق من وجود استبيان.
- الحسابات المولدة (توليد الحسابات) كانت تعدّي ولي الأمر مباشرة للبوابة بدون ملء بيانات أو استبيان.
- صفحة البيانات كانت تطلب اسم الطالب وصورته من ولي الأمر حتى لو الطالب مربوط بالفعل من التوليد.
- الطالب بحساب مولد كان يدخل مباشرة لصفحة الطالب بدون ملء بيانات.

الإصلاح:
- تم إصلاح `onboardingRequired: true` ← `false` في `student/new/page.tsx` بعد حفظ البيانات (كانت دي البق الأساسية).
- تم إعادة كتابة `redirectAfterLogin` في `auth/login/page.tsx`:
  - الطالب: لو اسمه placeholder أو `onboardingRequired=true` → صفحة إكمال البيانات.
  - ولي الأمر: لو مفيش طالب أو اسم الطالب placeholder → صفحة البيانات → ثم الاستبيان → ثم البوابة.
- تم إضافة redirect guards في `school-parent/page.tsx` و`parent/page.tsx`:
  - لو مفيش استبيان → redirect لصفحة الاستبيان.
  - لو مفيش طالب مربوط → redirect لصفحة البيانات.
- تم إضافة redirect guard في `school-student/page.tsx`:
  - لو اسم الطالب placeholder → redirect لصفحة إكمال البيانات.
- تم تحسين `student/new/page.tsx` للحسابات المولدة:
  - لو ولي الأمر عنده `linkedStudentId` من التوليد → الطالب يعتبر "مربوط" والفورم يعرض فقط حقول ولي الأمر (اسم ولي الأمر، الهاتف، الهوية، ملاحظات) بدون طلب اسم/صورة الطالب.

الملفات الأساسية:
- `src/app/auth/login/page.tsx` — إعادة كتابة redirectAfterLogin
- `src/app/student/new/page.tsx` — إصلاح onboardingRequired + دعم الحسابات المولدة
- `src/app/school-parent/page.tsx` — إضافة redirect guard للاستبيان
- `src/app/parent/page.tsx` — إضافة redirect guard للاستبيان
- `src/app/school-student/page.tsx` — إضافة redirect guard لإكمال البيانات

## 12. إصلاح تكرار التقارير السريرية وتراكمها

المشكلة:
- عند تكرار ملء الاستبيان أو خوض اختبار الطالب عدة مرات أثناء الاختبار، كانت دالة `saveReport` تنشئ وثيقة جديدة بمعرف عشوائي في كل مرة، مما أدى لتراكم 22 تقريراً مكرراً لنفس الطالب في لوحة أولياء الأمور (`/parents`) وظهور شارات تقارير زائدة في القائمة الجانبية (28 تقرير).

الإصلاح:
- تم تحديث `getReports()` في `cloudStore.ts` لدمج وحذف التقارير المكررة تلقائياً والاحتفاظ بأحدث وأكمل تقرير فقط لكل نوع لكل طالب، مع حذف النسخ المكررة الزائدة من السحابة `deleteDocFromCloud`.
- تم تعديل `saveReport()` ليفحص وجود تقرير سابق لنفس الطالب ونفس نوع التقرير ويحدّثه في مكانه بدلاً من إنشاء معرف جديد.
- تم ضبط `studentReports` في صفحة إدارة أولياء الأمور `parents/page.tsx` لتعرض الأنواع الأساسية الفريدة فقط (إجابات الاستبيان، التقرير التحليلي، إجابات الاختبار، تحليل الاختبار).

الملفات الأساسية:
- `src/lib/cloudStore.ts`
- `src/app/parents/page.tsx`

## التحقق

- تم تشغيل `npm run build` بنجاح بعد الإصلاحات.
- يتم النشر على Vercel وربط الإنتاج على `https://masarplatform.org`.
